# 全量依赖(开发用)。按层安装:
#   pip install -r requirements-core.txt        # 仅内核(推荐给库使用者)
#   pip install -r requirements-mcp.txt         # 内核 + MCP 服务
#   pip install -r requirements-assistant.txt   # 内核 + MCP + 产品层(全量)
-r requirements-assistant.txt
